parameters:
    redis.host: localhost
    redis.port: 6379

services: 
    client:
        class: \Redis
        calls:
            - [connect, ["%redis.host%", "%redis.port%"]]
    queue:
        class: \Teeparty\Queue\PHPRedis
        arguments: 
            client: @client
